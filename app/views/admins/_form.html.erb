<%= form_for(admin) do |f| %>
  <% if admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

      <ul>
      <% admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :admin_id %>
    <%= f.number_field :admin_id %>
  </div>

  <div class="field">
    <%= f.label :admin_code %>
    <%= f.number_field :admin_code %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
